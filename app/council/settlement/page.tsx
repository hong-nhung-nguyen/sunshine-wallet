import { Card } from "@/components/ui/card";
import { councilSettlement } from "@/lib/data/council";
import { formatAud } from "@/lib/formatters";

export default function SettlementPage(){return <div className="mx-auto max-w-7xl"><header><p className="text-sm font-semibold text-[var(--council-accent-strong,#956000)]">Settlement oversight</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-[var(--council-ink)] sm:text-4xl">Verified value allocation</h1><p className="mt-2 text-sm text-[var(--muted)]">Review the verified outcome and Council equity policy before credits are posted.</p></header>
  <section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card><p className="text-sm text-[var(--muted)]">Verified flexibility</p><p className="mt-3 font-mono text-3xl font-semibold">{councilSettlement.verifiedFlexEnergyKwh} kWh</p></Card><Card><p className="text-sm text-[var(--muted)]">Verification confidence</p><p className="mt-3 font-mono text-3xl font-semibold">{Math.round(councilSettlement.confidence*100)}%</p></Card><Card><p className="text-sm text-[var(--muted)]">Program value</p><p className="mt-3 font-mono text-3xl font-semibold">{formatAud(councilSettlement.totalValue)}</p></Card><Card><p className="text-sm text-[var(--muted)]">Status</p><p className="mt-3 text-3xl font-semibold capitalize">{councilSettlement.status}</p></Card></section>
  <Card className="mt-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold tracking-[0.12em] text-teal-700 uppercase">Allocation summary</p><h2 className="mt-2 text-2xl font-semibold">Council-approved distribution</h2></div><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs font-semibold text-emerald-900">Equity Floor passed</span></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-[var(--council-ink)] p-6 text-white"><p className="text-sm text-slate-300">Contributor rewards</p><p className="mt-2 font-mono text-4xl font-semibold">{formatAud(councilSettlement.contributorRewards)}</p><p className="mt-3 text-xs text-slate-400">For qualifying contribution to the verified event.</p></div><div className="rounded-2xl bg-amber-100 p-6 text-amber-950"><p className="text-sm text-amber-800">Equity Dividends</p><p className="mt-2 font-mono text-4xl font-semibold">{formatAud(councilSettlement.equityCredit)}</p><p className="mt-3 text-xs text-amber-800">Shared with eligible residents without practical roof access.</p></div></div><p className="mt-5 text-xs leading-5 text-[var(--muted)]">Calculated from simulated program value. This is not a real tariff, retailer settlement or guaranteed Council payment.</p></Card>
  </div>}
